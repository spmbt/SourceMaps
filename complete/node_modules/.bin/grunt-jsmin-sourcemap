#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jsmin-sourcemap/bin/grunt-jsmin-sourcemap" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jsmin-sourcemap/bin/grunt-jsmin-sourcemap" "$@"
  ret=$?
fi
exit $ret
